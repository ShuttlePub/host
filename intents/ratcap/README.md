# ratcap

Target repo: `ShuttlePub/shuttlepub-frontends` (旧 `ShuttlePub/RatCap` から 2026-08-30 rename。旧 URL は GitHub redirect で継続。アプリ資産は `apps/emumet-web/`)

Bootstrapped by `intent-cli intent init`.

Treat this as the parent host record for the `ratcap` domain. Add
upstream references and the canonical domain shape under
`intent-tree/`, durable Q/A under `interviews/`, and open
clarifications under `clarifications/open.md`.